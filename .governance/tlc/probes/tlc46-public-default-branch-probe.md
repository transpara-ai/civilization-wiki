# TLC 4.6 public default-branch probe

This inert document exists only to trigger the merged, read-only TLC 4.6 public audit workflow and prove that it starts a real job from the wiki default branch.

It grants no lifecycle credit, authority, enforcement, runtime, deployment, EventGraph, Hive, or other protected effect.
